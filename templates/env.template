# Copy to <contract_dir>/../.env, fill, chmod 600. Never commit.
# Load with:  set -a; source .env; set +a
#
# One prefix per system. The pack records the VARIABLE NAME in TARGET.md,
# never the value.
#
# EXAMPLE_TOKEN=
# EXAMPLE_DSN=
